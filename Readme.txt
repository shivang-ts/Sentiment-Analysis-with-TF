Created and trained a neural network model to classify movie reviews
taken from IMDB as either a positive review or a negative review.
The neural network model which when given a text review, is able to predict
if the overall sentiment of the review is more negative or more positive.
The dataset used is the IMDB reviews dataset available in Keras.
